---
description: Commit all changes and push to main branch
---

// turbo-all
1. Add all changes
   ```bash
   git add .
   ```

2. Commit with a timestamp
   ```bash
   git commit -m "Vibecode update $(date)"
   ```

3. Push to main
   ```bash
   git push origin main
   ```
